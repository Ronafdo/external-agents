# Domain docs

This is a single-context repository.

Before exploring, read the root `CONTEXT.md` and any relevant decisions in `docs/adr/`. Use the glossary's canonical vocabulary in specifications, tickets, and code. Surface a conflict with an ADR rather than silently overriding it.
